# Required metadata
sonar.projectKey=Sample-NodeJS
sonar.projectName=Sample-NodeJS
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=.

# Encoding of the source files
sonar.sourceEncoding=UTF-8
